/**
 * Demonstrate the new promises
 *  - Query a document and pass a callback that is both errback and callback
 *  (function(err, fn)). Make it err and make it successful
 *  - Query a document, get the writer, and execute it manually. Access the
 *  promise and append errbacks and callbacks manually (addErrback,
 *  addCallback)
 */
